git add <filename>   // for a particular file to be added to staging area

git add .     // for all files to be added to staging area

git commit -m "<Message>"
git commit

git commit -C HEAD --amend   // for changing the file with previous commit message

